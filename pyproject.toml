[project]
name = "httpxasync"
version = "0.1.0"
description = "httpx async problem exploration"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "psutil>=7.2.1",
]
